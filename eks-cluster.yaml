apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: flask-calculator-cluster
  region: us-east-1
  version: "1.30"

nodeGroups:
  - name: ng-1
    instanceType: c7i-flex.large
    desiredCapacity: 2
    minSize: 2
    maxSize: 2